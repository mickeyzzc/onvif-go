# SOAP Fixture Captures

The `*.xml` files here are minimal, hand-written SOAP envelopes used by the
client unit tests (GetStreamUri/GetSnapshotUri quirk matrix: normal, empty
URI, missing MediaUri wrapper, SOAP 1.1 prefixes).

They are synthetic: no real device traffic is committed to this repository.
